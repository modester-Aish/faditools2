'use client'

/**
 * Service worker registration disabled so the site always loads fresh data
 * from the server (no SW cache). Remove this component if not needed.
 */
export default function ServiceWorker() {
  return null
}
